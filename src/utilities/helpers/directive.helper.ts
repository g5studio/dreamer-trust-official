/**
 * this function do nothing, just to prevent variable not used error for directive
 */
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function registerDirective(fn: unknown) {}
